<!doctype html>
<html lang="en">
 <head>
  <meta charset="utf-8">
  <title>{{ site.title }}</title>
 </head>
 <body>
  <h1>{{ site.title }}</h1>
  <ul>
  {{#each site.images}}
   <li><a href="img/{{ this }}">{{ this }}</a></li>
  {{/each}}
  </ul>
 </body>
 </html>